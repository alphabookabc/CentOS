# Backup and Restore

# Backup MySQL
mysqldump --user root --password=123.com --databases MyDB > /backup/MySQL/MyDB-$(date +%Y%m%d).dump
# Restore MySQL
mysql --user root --password=123.com MyDB < /backup/MySQL/MyDB-$(date +%Y%m%d).dump
